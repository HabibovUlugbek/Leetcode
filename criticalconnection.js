var criticalConnections = function(n, connections) {
    var index = 0;
    var stack = [[]]
    connection.forEach(el => {
        if(el.index){
            strongconnect(el)
        }
    });

    function strongconnect(el)  {

        
    }
};